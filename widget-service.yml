server:
  port: 8030
spring:
  data:
    mongodb:
      host: ${MONGO_HOST:127.0.0.1}
      port: ${MONGO_PORT:27017}
      database: ${MONGO_DB:widget-development}

---

spring:
    profiles: development

---

spring:
    profiles: production
    data:
      mongodb:
        host: ${MONGO_HOST:127.0.0.1}
        port: ${MONGO_PORT:27017}
        database: ${MONGO_DB:widget-production}
server:
    port: 0
